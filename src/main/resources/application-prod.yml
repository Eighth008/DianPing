spring:
  datasource:
    url: jdbc:mysql://mysql:3306/dianping?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: redis
      port: 6379
  cache:
    caffeine:
      spec: maximumSize=500,expireAfterWrite=300s

redisson:
  address: redis://redis:6379